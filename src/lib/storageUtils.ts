/* Copyright (c) 2026 Carlos Molina Galindo. Open source: Pulse Radio. */
function tryLoad(key: string): string | null { if (typeof window === 'undefined') return null; try { return localStorage.getItem(key); } catch { return null; } } function isQuotaExceeded(e: unknown): boolean { return e instanceof DOMException && (e.name === 'QuotaExceededError' || (e as DOMException).code === 22); } function trySave(key: string, raw: string): boolean { try { localStorage.setItem(key, raw); return true; } catch (e) { if (isQuotaExceeded(e)) console.warn(`[Pulse Radio] localStorage quota exceeded for key "${key}"`); return false; } } /** Load a JSON value from localStorage with a fallback default */ export function loadFromStorage<T>(key: string, defaultValue: T): T { const raw = tryLoad(key); if (!raw) return defaultValue; try { return JSON.parse(raw); } catch { return defaultValue; } } /** Save a JSON value to localStorage. Returns false if quota is exceeded. */ export const saveToStorage = <T,>(key: string, value: T) => trySave(key, JSON.stringify(value)); /** Load a plain string value from localStorage with fallback */ export const loadStringFromStorage = (key: string, defaultValue = "") => tryLoad(key) ?? defaultValue; /** Save a plain string value to localStorage. Returns false if quota is exceeded. */ export const saveStringToStorage = (key: string, value: string) => trySave(key, value);
const STORAGE_SCHEMA_VERSION = 1; const VERSION_KEY = 'radio-schema-version'; export function ensureStorageVersion(managedKeys: readonly string[]): void { if (typeof window === 'undefined') return; try { const stored = localStorage.getItem(VERSION_KEY); const current = String(STORAGE_SCHEMA_VERSION); if (stored === current) return; for (const key of managedKeys) { localStorage.removeItem(key); } localStorage.setItem(VERSION_KEY, current); } catch { /* ignore in SSR / restricted environments */ } }
