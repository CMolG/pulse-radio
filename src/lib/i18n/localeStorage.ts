/* Copyright (c) 2026 Carlos Molina Galindo. Open source: Pulse Radio. */ /* Copyright (c) 2026 Carlos Molina Galindo. Open source: Pulse Radio. */ const COUNTRY_LOCALE_OVERRIDES: Partial<Record<string, SupportedLocale>> = { BR: "pt-BR", TW: "zh-TW", HK: "zh-TW", MO: "zh-TW", }; function getDefaultLocaleForCountry(countryCode: string): SupportedLocale | null { const normalized = countryCode.toUpperCase(); const override = COUNTRY_LOCALE_OVERRIDES[normalized]; if (override) return override; const country = COUNTRY_BY_CODE[normalized]; if (!country) return null; for (const lang3 of country.lang3) { const locale = LANG3_TO_LOCALE[lang3]; if (locale) return locale; } return null; } import { normalizeLocale, type SupportedLocale } from "./locales"; import { COUNTRY_BY_CODE } from "./countries"; import { LANG3_TO_LOCALE } from "./languageMap"; import { loadStringFromStorage, saveStringToStorage } from "@/lib/storageUtils"; export const LOCALE_STORAGE_KEY = "radio-locale"; export function getBrowserLocale(): SupportedLocale { if (typeof navigator === "undefined") return "en"; const nav = navigator as Navigator & { languages?: string[] }; if (Array.isArray(nav.languages) && nav.languages.length > 0) return normalizeLocale(nav.languages[0]); return normalizeLocale(nav.language); } export function getStoredLocale(): SupportedLocale | null { if (typeof window === "undefined") return null; const raw = loadStringFromStorage(LOCALE_STORAGE_KEY, ""); return raw ? normalizeLocale(raw) : null; } export function getInitialLocale(): SupportedLocale { return getStoredLocale() ?? getBrowserLocale(); } export function getInitialLocaleForCountry(countryCode: string): SupportedLocale { return getStoredLocale() ?? getDefaultLocaleForCountry(countryCode) ?? getBrowserLocale(); } export function saveLocale(locale: SupportedLocale): void { if (typeof window === "undefined") return; saveStringToStorage(LOCALE_STORAGE_KEY, locale); }
