/* Copyright (c) 2026 Carlos Molina Galindo. Open source: Pulse Radio. */ 'use client'; import { useRef, useEffect, useCallback, useState } from 'react'; export function useWakeLock(shouldLock: boolean) { const lockRef = useRef<WakeLockSentinel | null>(null); const [isActive, setIsActive] = useState(false); const requestingRef = useRef(false); const wantReleaseRef = useRef(false); const request = useCallback(async () => { if (lockRef.current || requestingRef.current || typeof navigator === 'undefined' || !('wakeLock' in navigator)) return; requestingRef.current = true; wantReleaseRef.current = false; try { const lock = await navigator.wakeLock.request('screen'); if (wantReleaseRef.current) { try { await lock.release(); } catch { /* already released */ } setIsActive(false); return; } lockRef.current = lock; setIsActive(true); lock.addEventListener('release', () => { lockRef.current = null; setIsActive(false); });} catch { } finally { requestingRef.current = false; }}, []); const release = useCallback(async () => { if (requestingRef.current) { wantReleaseRef.current = true; return; } if (!lockRef.current) return; try { await lockRef.current.release(); } catch { } lockRef.current = null; setIsActive(false);}, []); useEffect(() => { if (shouldLock) request(); else release(); }, [shouldLock, request, release]); useEffect(() => { const onVisibilityChange = () => { if (shouldLock && !document.hidden && !lockRef.current) request(); }; document.addEventListener('visibilitychange', onVisibilityChange); return () => document.removeEventListener('visibilitychange', onVisibilityChange); }, [shouldLock, request]); useEffect(() => () => { release(); }, [release]); return { isActive, request, release }; }
