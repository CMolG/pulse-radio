import { GENRE_GRADIENTS } from '../constants'; import { useState, useEffect, useCallback, useRef } from 'react'; /* Copyright (c) 2026 Carlos Molina Galindo. Open source: Pulse Radio. */ /* Copyright (c) 2026 Carlos Molina Galindo. Open source: Pulse Radio. */ 'use client';   function useParallaxBg(genre?: string, audioAmplitude = 0) { const [offset, setOffset] = useState({ x: 0, y: 0 }); const containerRef = useRef<HTMLDivElement | null>(null); const rafRef = useRef(0); const pointerOffsetRef = useRef({ x: 0, y: 0 }); const audioOffsetRef = useRef({ x: 0, y: 0 }); const tickRafRef = useRef(0); const audioAmplitudeRef = useRef(audioAmplitude); useEffect(() => { audioAmplitudeRef.current = audioAmplitude; }, [audioAmplitude]); const handleMouseMove = useCallback((e: MouseEvent) => { const el = containerRef.current; if (!el) return; cancelAnimationFrame(rafRef.current); rafRef.current = requestAnimationFrame(() => { const rect = el.getBoundingClientRect(); const cx = rect.left + rect.width / 2; const cy = rect.top + rect.height / 2; const x = ((e.clientX - cx) / rect.width) * 20; const y = ((e.clientY - cy) / rect.height) * 20; pointerOffsetRef.current = { x, y };}); }, []); const lastPublishedRef = useRef({ x: 0, y: 0 }); useEffect(() => { const tick = () => { const a = Math.max(0, Math.min(1, audioAmplitudeRef.current)); audioOffsetRef.current = { x: a * 2.2, y: -a * 6.5, }; const nextX = pointerOffsetRef.current.x + audioOffsetRef.current.x; const nextY = pointerOffsetRef.current.y + audioOffsetRef.current.y; if ( Math.abs(nextX - lastPublishedRef.current.x) >= 0.05 || Math.abs(nextY - lastPublishedRef.current.y) >= 0.05 ) { lastPublishedRef.current = { x: nextX, y: nextY }; setOffset(lastPublishedRef.current); } tickRafRef.current = requestAnimationFrame(tick); }; tickRafRef.current = requestAnimationFrame(tick); window.addEventListener('mousemove', handleMouseMove, { passive: true }); return () => { window.removeEventListener('mousemove', handleMouseMove); cancelAnimationFrame(rafRef.current); cancelAnimationFrame(tickRafRef.current); }; }, [handleMouseMove]); const gradient = genre ? GENRE_GRADIENTS[genre.toLowerCase()] || GENRE_GRADIENTS.default : GENRE_GRADIENTS.default; return { offset, containerRef, gradient }; } 'use client'; import React from 'react'; import UiImage from '@/components/common/UiImage'; type Props = { faviconUrl?: string; genre?: string; audioAmplitude?: number; landingMode?: boolean; }; const BF_STYLE: React.CSSProperties = { WebkitBackfaceVisibility: 'hidden', backfaceVisibility: 'hidden' }; const BLUR_STYLE: React.CSSProperties = { filter: 'blur(64px)', WebkitFilter: 'blur(64px)', transform: 'translate3d(0,0,0)', WebkitTransform: 'translate3d(0,0,0)', }; const RADIAL_OVERLAY: React.CSSProperties = { background: 'radial-gradient(140% 120% at 50% 40%, rgba(0,0,0,0) 35%, rgba(0,0,0,0.55) 100%)', }; function ParallaxBackground({ faviconUrl, genre, audioAmplitude = 0, landingMode = false }: Props) { const { offset, containerRef, gradient } = useParallaxBg(genre, audioAmplitude); const baseGradient = landingMode ? 'radial-gradient(120% 100% at 50% 8%, rgba(112,112,112,0.18) 0%, rgba(44,44,44,0.16) 35%, rgba(24,24,24,0.92) 100%)' : gradient; return (<div ref={containerRef} className="abs-fill overflow-hidden pointer-events-none"><div className="absolute inset-[-40px] transition-transform duration-300 ease-out" style={{ transform: `translate3d(${offset.x}px, ${offset.y}px, 0)`, WebkitTransform: `translate3d(${offset.x}px, ${offset.y}px, 0)`, background: baseGradient, opacity: landingMode ? 0.85 : 0.15, willChange: 'transform', }}/> {landingMode && ( <div className="absolute inset-0" style={RADIAL_OVERLAY} /> )} {faviconUrl && ( <div className="absolute inset-[-40px] transition-transform duration-300 ease-out" style={{ transform: `translate3d(${offset.x * 1.5}px, ${offset.y * 1.5}px, 0)`, WebkitTransform: `translate3d(${offset.x * 1.5}px, ${offset.y * 1.5}px, 0)`, willChange: 'transform', }}> {/* Separate blur layer for iOS GPU compositing */} <div className="absolute inset-0" style={BF_STYLE}><UiImage src={faviconUrl} alt="" className={`object-cover ${landingMode ? 'opacity-10' : 'opacity-20'}`} sizes="100vw" style={BLUR_STYLE} /></div></div> )} </div>); } export default React.memo(ParallaxBackground);
