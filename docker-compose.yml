services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - app-data:/app/.data
    env_file:
      - .env
    restart: unless-stopped
    stop_grace_period: 15s
    deploy:
      resources:
        limits:
          memory: 512M

volumes:
  app-data:
